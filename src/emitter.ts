import { IMethod, IProperty } from "./interfaces"

export function emitSingleClass(name: string, properties: IProperty[], methods: IProperty[]) {
    return null
}

export function emitSingleInterface(name: string, properties: IProperty[], methods: IProperty[]) {
    return null
}
